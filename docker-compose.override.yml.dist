version: '3.7'

services:
  php:
    environment:
      VIRTUAL_HOST: dummy-a.sebastianthoss.de
#      LETSENCRYPT_HOST: dummy-a.sebastianthoss.de
#      LETSENCRYPT_EMAIL: MAIL_ADDRESS

networks:
  default:
    name: docker-dev-nginx
    driver: bridge